# Backend Programming Template (2025)
# Revisi 1

# Perubahan yang dilakukan adalah:

1. Pengisian function changePassword untuk endpoint PUT api/users/:id/change-password (perubahan di users-repository, users-service, dan users-controller)

2. Perbaikan function create untuk endpoint GET api/books/ (di books-controller)

3. Penambahan function getBook untuk endpoint GET api/books/:id (perubahan di books-repository, books-service, dan books-controller)

4. Dan beberapa perubahan kecil lainnya